import tw, { styled } from "twin.macro"

export const Info = styled.div`
  ${tw`flex items-center text-gray-700 font-light text-sm`}
  svg {
    ${tw`text-primary `}
  }
`

export const InfoIcon = tw.div` mr-2`

export const InfoText = tw.div``
